Wired PL/SW compiler pipeline to run on COR24 emulator via WASM. Created pipeline.rs module that assembles the embedded plsw.s compiler, boots it on EmulatorCore (running self-tests), feeds source code via UART RX with VecDeque-based byte queue, and captures token output via UART TX. Updated build.rs to embed compiler binary from sibling project. Added compilation trigger on wizard Preprocess advance with async timeout for UI responsiveness. Replaced placeholder cells with real pipeline output display showing boot self-tests and lexer token output, with status indicators and error handling. Added CSS styling for output cells, spinner, error display. Compiler currently implements lexer stage only; UI notes this and is ready for full compilation when parser/codegen are complete in sw-cor24-plsw.