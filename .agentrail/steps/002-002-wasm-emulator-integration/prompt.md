Wire the cor24-emulator dependency and verify WASM integration. Import EmulatorCore and Assembler from cor24-emulator. Create a minimal smoke test in lib.rs: assemble a trivial COR24 program (e.g. MOV r0, #42 / HLT), load into EmulatorCore, step, read r0 to verify value. Add a use_effect_with in App that runs this test on mount and logs the result to web_sys::console. Verify trunk build succeeds and the console output appears in the browser. This confirms the emulator runs correctly in WASM before building the full UI.