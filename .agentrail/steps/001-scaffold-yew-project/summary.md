Scaffolded Yew 0.21 CSR project with Trunk build system. Created Cargo.toml (edition 2024, cdylib+rlib, cor24-emulator/cor24-isa path deps, yew/wasm-bindgen/web-sys/gloo/console_error_panic_hook deps), index.html (Catppuccin Mocha full palette, flexbox 100vh layout, GitHub corner SVG), src/main.rs (panic hook + Yew renderer), src/lib.rs (App component with header/footer/build metadata), src/app.css, build.rs (BUILD_SHA/HOST/TIMESTAMP), Trunk.toml (port 9507), scripts/serve.sh and build-pages.sh, .gitignore (target+dist), pages/.nojekyll. Release profile: opt-level z, lto true. Verified: cargo clippy -D warnings passes, cargo fmt clean, trunk build succeeds.