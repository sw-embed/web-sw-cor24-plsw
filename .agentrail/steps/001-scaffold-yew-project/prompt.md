Scaffold the Yew/WASM project with Trunk build system. Update Cargo.toml: edition 2024, Yew 0.21 (csr), wasm-bindgen, web-sys (console, HtmlTextAreaElement, HtmlInputElement, HtmlSelectElement, HtmlElement, KeyboardEvent), gloo 0.11, console_error_panic_hook, cor24-emulator path dep (../sw-cor24-emulator, default-features = false), cor24-isa path dep (../sw-cor24-emulator/isa, features = ["serde"]). Set crate-type = ["cdylib", "rlib"]. Create index.html with Catppuccin Mocha theme (full color palette as CSS custom properties), Trunk directives (data-trunk rel=rust, data-trunk rel=css), monospace font stack (JetBrains Mono, Fira Code, Cascadia Code), flexbox 100vh layout with header/footer, GitHub corner SVG. Create src/main.rs (console_error_panic_hook::set_once + yew::Renderer). Create src/lib.rs with App function_component (header with title 'PL/SW -- COR24 Dev', footer with MIT license, copyright 2026, build metadata via env\! macros). Create Trunk.toml (dist dir, port 9507). Create scripts/serve.sh (port 9507) and scripts/build-pages.sh (--release --public-url /web-sw-cor24-plsw/, rsync to pages/, .nojekyll). Create build.rs for BUILD_SHA, BUILD_HOST, BUILD_TIMESTAMP. Create .gitignore entries for dist/ and target/. Add release profile: opt-level = z, lto = true. Verify trunk build succeeds and ./scripts/serve.sh serves the page. Pattern reference: ~/github/sw-embed/web-sw-cor24-pcode for structure.